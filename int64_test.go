// generated by github.com/schigh/slices/internal/base.go.  do not edit.
package slices

import (
	"testing"
	"reflect"
	"github.com/schigh/slices/internal"
)

//region TESTS
func TestInt64Slice_IndexOf(t *testing.T) {
	tests := []struct {
		name string
		slice []int64
		needle int64
		expected int
	}{
		{
			name: "only item",
			slice: []int64{1},
			needle: 1,
			expected: 0,
		},
		{
			name: "at index 1",
			slice: []int64{0,1,1},
			needle: 1,
			expected: 1,
		},
		{
			name: "missing",
			slice: []int64{1,2,3,4},
			needle: 5,
			expected: NotInSlice,
		},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			result := Int64Slice(test.slice).IndexOf(test.needle)
			if result != test.expected {
				t.Errorf("expected %d, got %d", test.expected, result)
			}
		})
	}
}

func TestInt64Slice_Contains(t *testing.T) {
	tests := []struct {
		name string
		slice []int64
		needle int64
		expected bool
	}{
		{
			name: "present",
			slice: []int64{1,2,3,4},
			needle: 4,
			expected: true,
		},
		{
			name: "not present",
			slice: []int64{1,2,3,4},
			needle: 5,
			expected: false,
		},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			result := Int64Slice(test.slice).Contains(test.needle)
			if result != test.expected {
				t.Errorf("expected %v, got %v", test.expected, result)
			}
		})
	}
}

func TestInt64Slice_SortAsc(t *testing.T) {
	tests := []struct {
		name string
		slice []int64
		expected []int64
	}{
		{
			name: "empty",
			slice: []int64{},
			expected: []int64{},
		},
		{
			name: "already sorted",
			slice: []int64{0,1,2,3,4,5},
			expected: []int64{0,1,2,3,4,5},
		},
		{
			name: "reversed",
			slice: []int64{5,4,3,2,1,0},
			expected: []int64{0,1,2,3,4,5},
		},
		{
			name: "random",
			slice: []int64{3,1,4,5,0,2},
			expected: []int64{0,1,2,3,4,5},
		},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			result := Int64Slice(test.slice).SortAsc().Value()
			if !reflect.DeepEqual(test.expected, result) {
				t.Errorf("expected %v, got %v", test.expected, result)
			}
		})
	}
}

func TestInt64Slice_SortDesc(t *testing.T) {
	tests := []struct {
		name string
		slice []int64
		expected []int64
	}{
		{
			name: "empty",
			slice: []int64{},
			expected: []int64{},
		},
		{
			name: "already sorted",
			slice: []int64{5,4,3,2,1,0},
			expected: []int64{5,4,3,2,1,0},
		},
		{
			name: "reversed",
			slice: []int64{0,1,2,3,4,5},
			expected: []int64{5,4,3,2,1,0},
		},
		{
			name: "random",
			slice: []int64{3,1,4,5,0,2},
			expected: []int64{5,4,3,2,1,0},
		},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			result := Int64Slice(test.slice).SortDesc().Value()
			if !reflect.DeepEqual(test.expected, result) {
				t.Errorf("expected %v, got %v", test.expected, result)
			}
		})
	}
}

func TestInt64Slice_Unique(t *testing.T) {
	tests := []struct {
		name string
		slice []int64
		expected []int64
	}{
		{
			name: "unaffected",
			slice: []int64{0,1,2,3,4,5},
			expected: []int64{0,1,2,3,4,5},
		},
		{
			name: "one extra five",
			slice: []int64{5,0,1,2,3,4,5},
			expected: []int64{5,0,1,2,3,4},
		},
		{
			name: "extras everywhere",
			slice: []int64{0,0,1,0,1,2,2,2,3,0,3,4,2,3,4,4,2,1,0},
			expected: []int64{0,1,2,3,4},
		},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			result := Int64Slice(test.slice).Unique().Value()
			if !reflect.DeepEqual(test.expected, result) {
				t.Errorf("expected %v, got %v", test.expected, result)
			}
		})
	}
}

func TestInt64Slice_Reverse(t *testing.T) {
	tests := []struct {
		name string
		slice []int64
		expected []int64
	}{
		{
			name: "even length",
			slice: []int64{0,1,2,3,4,5},
			expected: []int64{5,4,3,2,1,0},
		},
		{
			name: "odd length",
			slice: []int64{0,1,2,3,4,5,6},
			expected: []int64{6,5,4,3,2,1,0},
		},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			result := Int64Slice(test.slice).Reverse().Value()
			if !reflect.DeepEqual(test.expected, result) {
				t.Errorf("expected %v, got %v", test.expected, result)
			}
		})
	}
}

func TestInt64Slice_Filter(t *testing.T) {
	tests := []struct {
		name string
		slice []int64
		expected []int64
		filterFunc func(int64) bool
	}{
		{
			name: "gt 10",
			slice: []int64{1, 2, 5, 11, 13, 15},
			expected: []int64{11, 13, 15},
			filterFunc: func(n int64) bool { return n > 10 },
		},
		{
			name: "mod 3",
			slice: []int64{1, 2, 6, 11, 12, 15, 17},
			expected: []int64{6, 12, 15},
			filterFunc: func(n int64) bool { return n%3 == 0 },
		},
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
			result := Int64Slice(test.slice).Filter(test.filterFunc).Value()
			if !reflect.DeepEqual(test.expected, result) {
				t.Errorf("expected %v, got %v", test.expected, result)
			}
		})
	}
}

//endregion

//region BENCHMARKS

func BenchmarkInt64Slice_IndexOf(b *testing.B) {
	benchmarks := []struct {
		name string
		slice []int64
	}{
		{
			name: "10 elements",
			slice: internal.GenInt64Slice(10),
		},
		{
			name: "100 elements",
			slice: internal.GenInt64Slice(100),
		},
		{
			name: "1000 elements",
			slice: internal.GenInt64Slice(1000),
		},
		{
			name: "10000 elements",
			slice: internal.GenInt64Slice(10000),
		},
		{
			name: "100000 elements",
			slice: internal.GenInt64Slice(100000),
		},
	}
	for _, bm := range benchmarks {
		b.Run(bm.name, func(b *testing.B) {
			for i := 0; i < b.N; i++ {
				Int64Slice(bm.slice).IndexOf(1)
			}
		})
	}
}

func BenchmarkInt64Slice_Contains(b *testing.B) {
	benchmarks := []struct {
		name string
		slice []int64
	}{
		{
			name: "10 elements",
			slice: internal.GenInt64Slice(10),
		},
		{
			name: "100 elements",
			slice: internal.GenInt64Slice(100),
		},
		{
			name: "1000 elements",
			slice: internal.GenInt64Slice(1000),
		},
		{
			name: "10000 elements",
			slice: internal.GenInt64Slice(10000),
		},
		{
			name: "100000 elements",
			slice: internal.GenInt64Slice(100000),
		},
	}
	for _, bm := range benchmarks {
		b.Run(bm.name, func(b *testing.B) {
			for i := 0; i < b.N; i++ {
				Int64Slice(bm.slice).Contains(1)
			}
		})
	}
}

func BenchmarkInt64Slice_SortAsc(b *testing.B) {
	benchmarks := []struct {
		name string
		slice []int64
	}{
		{
			name: "10 elements",
			slice: internal.GenInt64Slice(10),
		},
		{
			name: "100 elements",
			slice: internal.GenInt64Slice(100),
		},
		{
			name: "1000 elements",
			slice: internal.GenInt64Slice(1000),
		},
		{
			name: "10000 elements",
			slice: internal.GenInt64Slice(10000),
		},
		{
			name: "100000 elements",
			slice: internal.GenInt64Slice(100000),
		},
	}
	for _, bm := range benchmarks {
		b.Run(bm.name, func(b *testing.B) {
			for i := 0; i < b.N; i++ {
				Int64Slice(bm.slice).SortAsc()
			}
		})
	}
}

func BenchmarkInt64Slice_SortDesc(b *testing.B) {
	benchmarks := []struct {
		name string
		slice []int64
	}{
		{
			name: "10 elements",
			slice: internal.GenInt64Slice(10),
		},
		{
			name: "100 elements",
			slice: internal.GenInt64Slice(100),
		},
		{
			name: "1000 elements",
			slice: internal.GenInt64Slice(1000),
		},
		{
			name: "10000 elements",
			slice: internal.GenInt64Slice(10000),
		},
		{
			name: "100000 elements",
			slice: internal.GenInt64Slice(100000),
		},
	}
	for _, bm := range benchmarks {
		b.Run(bm.name, func(b *testing.B) {
			for i := 0; i < b.N; i++ {
				Int64Slice(bm.slice).SortDesc()
			}
		})
	}
}

func BenchmarkInt64Slice_Reverse(b *testing.B) {
	benchmarks := []struct {
		name string
		slice []int64
	}{
		{
			name: "10 elements",
			slice: internal.GenInt64Slice(10),
		},
		{
			name: "100 elements",
			slice: internal.GenInt64Slice(100),
		},
		{
			name: "1000 elements",
			slice: internal.GenInt64Slice(1000),
		},
		{
			name: "10000 elements",
			slice: internal.GenInt64Slice(10000),
		},
		{
			name: "100000 elements",
			slice: internal.GenInt64Slice(100000),
		},
	}
	for _, bm := range benchmarks {
		b.Run(bm.name, func(b *testing.B) {
			for i := 0; i < b.N; i++ {
				Int64Slice(bm.slice).Reverse()
			}
		})
	}
}
//endregion
